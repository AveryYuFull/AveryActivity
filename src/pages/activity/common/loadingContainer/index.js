import AcLoadingContainer from './LoadingContainer';

AcLoadingContainer.install = function (Vue) {
    Vue.component(AcLoadingContainer.name, AcLoadingContainer);
};

export default AcLoadingContainer;
